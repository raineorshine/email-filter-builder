{
  "name": "email-filter-builder",
  "description": "Generates email filters from a simple JSON specification. Outputs Sieve scripts (ProtonMail) and Gmail's XML import format.",
  "version": "0.0.0",
  "author": {
    "name": "Raine Revere",
    "email": "raine.revere@protonmail.com",
    "url": "https://github.com/raineorshine"
  },
  "license": "ISC",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "node build.js",
    "format": "prettier --write .",
    "sync": "node sync.js",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "jest": "^29.0.3",
    "prettier": "^2.8.4"
  }
}
